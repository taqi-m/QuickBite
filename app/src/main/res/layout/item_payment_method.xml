<?xml version="1.0" encoding="utf-8"?>
<!-- L4: RelativeLayout for Payment Method Option -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/selector_payment_method"
    android:clickable="true"
    android:focusable="true"
    android:minHeight="72dp"
    android:paddingHorizontal="@dimen/spacing_lg"
    android:paddingVertical="@dimen/spacing_lg">

    <!-- Icon Container -->
    <FrameLayout
        android:id="@+id/fl_payment_icon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:background="@drawable/bg_payment_icon_unselected">

        <ImageView
            android:id="@+id/iv_payment_icon"
            android:layout_width="@dimen/icon_size_medium"
            android:layout_height="@dimen/icon_size_medium"
            android:layout_gravity="center"
            android:contentDescription="@string/payment_method"
            tools:src="@drawable/ic_credit_card_128"
            tools:tint="@color/grey_text" />
    </FrameLayout>

    <!-- Payment Details -->
    <LinearLayout
        android:id="@+id/ll_payment_details"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginStart="@dimen/spacing_lg"
        android:layout_toStartOf="@id/iv_check_circle"
        android:layout_toEndOf="@id/fl_payment_icon"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_payment_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-black"
            android:textColor="@color/navy"
            android:textSize="@dimen/text_size_subhead"
            tools:text="Credit / Debit Card" />

        <TextView
            android:id="@+id/tv_payment_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:fontFamily="sans-serif"
            android:textColor="@color/grey_text"
            android:textSize="12sp"
            tools:text="Visa, Mastercard, Amex" />
    </LinearLayout>

    <!-- Check Circle (for selected state) -->
    <ImageView
        android:id="@+id/iv_check_circle"
        android:layout_width="@dimen/icon_size_medium"
        android:layout_height="@dimen/icon_size_medium"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:contentDescription="@string/cd_back_button"
        android:src="@drawable/ic_check_circle_128"
        app:tint="@color/primary"
        android:visibility="gone"
        tools:visibility="visible" />
</RelativeLayout>

